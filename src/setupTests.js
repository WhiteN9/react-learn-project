/* The jest-dom library adds custom matchers for performing assertions 
on DOM nodes (such as toHaveTextContent() or toBeInTheDocument()), 
which you will learn about later on in this lesson.*/
import "@testing-library/jest-dom"